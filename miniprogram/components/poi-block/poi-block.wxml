<!--
  POI Block - 地点块组件
  复用原有的毛玻璃活动卡片样式
-->
<view class="poi-block {{editMode ? 'edit-mode' : ''}}">
  <!-- 删除按钮 (仅编辑模式可见) -->
  <view class="delete-btn" wx:if="{{editMode}}" catchtap="onDeleteTap">
    <text class="delete-icon">×</text>
  </view>

  <!-- 元信息：时间/时长/费用 -->
  <view class="card-meta">
    <view class="meta-item">
      <text class="icon">⏰</text>
      <input wx:if="{{editMode}}" class="meta-input" value="{{block.content.startTime || block.content.time}}" bindblur="onInputBlur" data-field="startTime" placeholder="10:00"/>
      <text wx:else>{{block.content.startTime || block.content.time || '时间'}}</text>
    </view>
    <text class="dot">·</text>
    <view class="meta-item">
      <text class="icon">⏳</text>
      <input wx:if="{{editMode}}" class="meta-input" value="{{block.content.duration}}" bindblur="onInputBlur" data-field="duration" placeholder="60"/>
      <text wx:else>{{formattedDuration || (block.content.duration ? (block.content.duration + '分钟') : '')}}</text>
    </view>
    <text class="dot" wx:if="{{block.content.cost || editMode}}">·</text>
    <view class="meta-item" wx:if="{{block.content.cost || editMode}}">
      <text class="icon">💰</text>
      <input wx:if="{{editMode}}" class="meta-input" value="{{block.content.cost}}" bindblur="onInputBlur" data-field="cost" placeholder="0"/>
      <text wx:else>¥{{block.content.cost}}</text>
    </view>
  </view>

  <!-- 主内容区 -->
  <view class="main-content">
    <!-- 名称和导航按钮 -->
    <view class="name-row">
      <input wx:if="{{editMode}}" class="poi-input title" value="{{block.content.name}}" bindblur="onInputBlur" data-field="name" placeholder="地点名称"/>
      <text wx:else class="poi-name">{{block.content.name}}</text>
      <view class="nav-btn" bindtap="onNavTap" wx:if="{{!editMode}}">
        <view class="nav-icon-bg">
          <text class="nav-icon">↗</text>
        </view>
        <text class="nav-text">去这里</text>
      </view>
    </view>

    <!-- 描述/地址 (可编辑) -->
    <view class="desc-wrapper">
      <textarea
        wx:if="{{editMode}}"
        class="desc-input"
        auto-height
        maxlength="200"
        placeholder="点击编辑活动描述..."
        value="{{block.content.description || block.content.address}}"
        bindinput="onDescInput"
        bindblur="onDescBlur"
      />
      <text wx:else class="desc-text">{{block.content.description || block.content.address || '暂无描述'}}</text>
    </view>


    <!-- 标签 -->
    <view class="tags-row" wx:if="{{block.content.tags && block.content.tags.length > 0}}">
      <text class="tag" wx:for="{{block.content.tags}}" wx:key="*this">{{item}}</text>
    </view>

    <!-- 备注 -->
    <view class="card-note">
      <input wx:if="{{editMode}}" class="note-input" value="{{block.content.note}}" bindblur="onInputBlur" data-field="note" placeholder="添加备注 (如: 提前买票)"/>
      <text wx:elif="{{block.content.note}}" class="note-text">💡 {{block.content.note}}</text>
    </view>
  </view>
</view>

