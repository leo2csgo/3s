<!--pages/discover/discover.wxml-->
<view class="container aurora-page">
  <view class="header">
    <text class="title">发现灵感</text>
    <text class="subtitle">开启你的下一段旅程</text>
  </view>

  <view class="action-row">
    <view class="btn-ai glass-strong" bindtap="goToAIGenerate" hover-class="pressed">
      <text class="icon-sparkles">✨</text>
      <text>AI 魔法生成</text>
    </view>

    <view class="btn-blank glass-strong" bindtap="createBlankTrip" hover-class="pressed">
      <text class="icon-plus">＋</text>
    </view>
  </view>


  <!-- 筛选 chips -->

  <view class="section">
    <view class="section-header">
      <text class="icon-trend">🔥</text>
      <text class="section-title">热门模板</text>
    </view>

    <view class="grid">
      <block wx:for="{{templates}}" wx:key="id">
        <view class="card glass {{index % 3 === 0 ? 'card-wide' : ''}}" hover-class="pressed"
              bindtap="useTemplate" data-id="{{item.id}}">

          <view class="card-image-wrap">
            <image src="{{item.cover}}" mode="aspectFill" class="card-image" />
            <view class="days-badge glass-strong">{{item.days}}天</view>
          </view>

          <view class="card-content">
            <text class="card-title">{{item.title}}</text>
            <view class="tags">
              <text wx:for="{{item.tags}}" wx:for-item="tag" wx:key="*this" class="tag">{{tag}}</text>
            </view>
            <view class="likes">
              <text>❤️ {{1234 + index * 100}}</text> </view>
          </view>
        </view>
      </block>
    </view>
  </view>

</view>
